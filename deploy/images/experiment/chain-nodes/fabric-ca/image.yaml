# image.yaml
name: fabric-ca
module: experiment
category: chain-nodes
runtime: fabric
ports:
  - 7054/tcp
resource_recommendation:
  cpu: 0.25
  memory: 256Mi
required_dependencies: []
typical_companions:
  required: []
  recommended:
    - image: fabric-peer
      reason: 提供证书与身份管理
  optional: []
default_ports:
  - port: 7054
    protocol: tcp
    name: HTTP
description: Hyperledger Fabric CA 证书服务镜像。
