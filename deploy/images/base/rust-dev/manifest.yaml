name: rust-dev
category: base
ecosystem: polkadot
description: Rust 区块链开发环境镜像
maintainer: lenschain-platform
license: MIT
official_source: lenschain/rust-dev
registry_project: base

versions:
  - version: "1.0"
    tag: "v1.0.0"
    upstream_tag: "v1.0.0"
    min_cpu: "0.25"
    min_memory: "512Mi"
    recommended_cpu: "0.5"
    recommended_memory: "1Gi"
    is_default: true

default_ports: []
default_env_vars: []
conditional_env_vars: []

default_volumes:
  - mount_path: /home/developer/project
    purpose: project_data
    size: 5Gi
    description: 项目工作目录

typical_companions:
  required: []
  recommended:
    - image: substrate-node
      reason: Substrate 节点
  optional: []

required_dependencies: []
supported_tools: []

healthcheck:
  test: ["CMD", "rustc", "--version"]
  interval: 60s
  timeout: 5s
  retries: 1
  start_period: 5s

documentation_url: /docs/images/rust-dev
prepull_phase: 1
prepull_node_tier: light

security_scan:
  enabled: true
  allow_critical: false
  allow_high: true
