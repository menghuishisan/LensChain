apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: lenschain
spec:
  replicas: 1
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
