# deploy/k8s/overlays/staging/kustomization.yaml
# staging 环境 overlay
# 在 base 基础上叠加测试规格和部分预拉取镜像配置
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: backend-patch.yaml
  - path: image-prepuller-configmap.yaml
