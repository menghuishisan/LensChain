apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-chain-${TEAM_ID}
  namespace: ${NAMESPACE}
  labels:
    lenschain.io/runtime-type: ctf-attack-defense
    lenschain.io/runtime-role: team-chain
    lenschain.io/team-id: ${TEAM_ID}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: team-chain-${TEAM_ID}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: team-chain-${TEAM_ID}
        lenschain.io/runtime-role: team-chain
        lenschain.io/team-id: ${TEAM_ID}
    spec:
      containers:
        - name: team-chain
          image: ${TEAM_CHAIN_IMAGE}
          imagePullPolicy: IfNotPresent
