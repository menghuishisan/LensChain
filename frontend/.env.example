# .env.example
# 链镜平台前端示例环境变量文件
# 用途：
#   1. 作为本地开发、测试、部署时的环境变量模板
#   2. 复制为 .env.local（本地开发）或 .env.production（生产构建）后按需修改
#   3. 所有暴露到浏览器的变量必须以 NEXT_PUBLIC_ 开头，否则 Next.js 在客户端无法读取
# 注意：
#   - 本文件仅作为示例，不应包含任何真实密钥
#   - .env.local 已在 .gitignore 中忽略，请勿提交

# NEXT_PUBLIC_API_BASE_URL: 后端 API 基础地址
# - 前端所有 HTTP 请求（api-client）均基于此地址拼接路径
# - 必须包含 /api/v1 路径前缀，与后端路由保持一致
# - 本地开发一般指向后端服务的本地端口；生产环境指向部署后的 API 网关地址
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1

# NEXT_PUBLIC_WS_BASE_URL: 后端 WebSocket 基础地址
# - 前端所有实时连接（CTF / 实验 / 通知 / SimEngine / 终端）均基于此地址拼接路径
# - 协议必须显式指定：http:// 会自动转换为 ws://，https:// 会自动转换为 wss://
# - 与 API_BASE_URL 分离，便于生产环境将 WebSocket 路由到独立网关（如反代 TLS 卸载、WS 专用限流）
# - 本地开发通常与 API 同主机同端口；生产环境按部署架构调整
NEXT_PUBLIC_WS_BASE_URL=http://localhost:8080/api/v1
